#include "LibraryUtilities/PropertyDifference.hxx"

// PropertyDifference is header-only (inline value helpers + templated collectors).
// This translation unit exists only to verify the header is self-contained.
